MANDATORY:

-   wc -l, ls -l
-   ls -l, head -n 5

Invalid cmnd:

-   ls, shesh
-   shesh, wc

Invalid Infile + Invalid cmnd:

1. No File:
-   ls, wc
-   hi, wc
-   wc, hi
-   hi, hi

2. No permission (read) on infile
-   ls, wc
-   hi, wc
-   wc, hi
-   hi, hi

3. The infile is a folder
-   ls, wc
-   hi, wc
-   wc, hi
-   hi, hi

4. No permission (write) on outfile + Invalid cmnd
-   ls, wc     normal
-   hi, wc     command not found : hi
-   wc, hi     normal         
-   hi, lol    command not found : hi

        + Invalid Infile
        -   ls, wc     permission!
        -   wc, head   permission! + directory
        -   hi, wc     permission! + command not found; hi
        -   wc, hi     permission! + directory
        -   hi, lol    permission! + command not found: hi  



HEREDOC: outfile append ???

-   Permission outfile
-   Permission outfile + Invalid cmnd
-   !!! head -n -5 !!!


EDGE:

-   /dev/stdin
        - invalid cmnd
        - bonus
        - cat wc -l
        - cat ls -l
        - head  && head -n 5 !!!

-   /dev/urandom
        - invalid cmnd
        - bonus

-   cmnd_path:
        - /usr/bin/head
        - /bin/ls
        - //bin/ls

        - Invalid
            - usr/bin/head
            - /usr/head
        - first cmnd /hi + no (write) permission > outfile !!!
        



-   PATH="" / unset PATH
    -  Invalid Infile + Invalid cmnd + Invalid write

    infolder wc | ls outpermission   if ( && !cmnd && last == acc)
        - command not found: wc
        - permission denied: outfile

